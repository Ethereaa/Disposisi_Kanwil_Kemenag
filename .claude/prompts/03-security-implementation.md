Implement ONLY the approved security improvements.

Rules:

- Work one fix at a time.
- After each fix:
  - Explain what changed.
  - Explain why it is safe.
  - Show the files modified.
  - Wait for my approval before continuing.

Do not bundle multiple fixes together.

Do not refactor unrelated code.

Do not improve formatting unless required.

Preserve all existing functionality.

Create migrations when database changes are required.

After each completed fix, recommend a git commit message.

Never continue automatically to the next fix.